# Сборка
build/
.gradle/
local.properties
.kotlin/

# IDE
.idea/
*.iml
.vscode/

# Ключи подписи живут ВНЕ репозитория и никогда не коммитятся.
keystore.properties
*.keystore
*.jks

# Выгрузки с устройства: содержат адреса серверов, ключи потоков и токены
# виджетов. Никогда не попадают в историю — ни в публичную, ни в приватную.
diag/
debug/

# Локальные заметки
*.local.md
